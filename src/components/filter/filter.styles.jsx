import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
  title: {
    margin: 'auto 5px',
    color: '#126',
    fontWeight: '600',
    fontSize: '1.4rem'
  }
});
