import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
  root: {
    width: '80%',
    padding: 0,
    margin: '0 50px',
    marginBottom: '15px'
  },
  media: {
    width: '100%',
    height: 'auto'
  }
});
