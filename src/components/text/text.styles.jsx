import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
  label: {
    fontSize: '1rem',
    margin: '10px 0'
  },
  details: {
    fontWeight: '300',
    marginBottom: '15px'
  }
});
